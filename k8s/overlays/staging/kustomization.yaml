apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Staging mirrors production topology at reduced scale: base replica
# counts apply unchanged and the HPAs handle bursts.
resources:
  - ../../base
